[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "blaecktcmpy"
version = "1.0.0"
description = "A MicroPython BlaeckTCP server library"
license = "MIT"

[tool.setuptools.packages.find]
where = ["src"]
